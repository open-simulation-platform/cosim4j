
println "Gradle version is ${gradle.getGradleVersion()}"

wrapper {
    distributionType = Wrapper.DistributionType.ALL
}

subprojects {
    group 'org.osp'
    version '0.2.0-SNAPSHOT'
}

def win = new File(rootDir, "natives-win/src/main/resources/native/win")
if (win.exists()) {
    println(Arrays.asList(win.list()))
}

def linux = new File(rootDir, "natives-linux/src/main/resources/native/linux")
if (linux.exists()) {
    println(Arrays.asList(win.list()))
}
