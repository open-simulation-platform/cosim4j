
def nativesFolder = project.ext.nativesFolder
if (nativesFolder.listFiles() != null && nativesFolder.listFiles().length > 0) {

    publishing {

        publications {
            maven(MavenPublication) {
                from components.java
            }
        }
    }
}
